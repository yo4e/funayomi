# AGENTS.md

このファイルは、FunaYomiを会話履歴なしで再開するAI共同作業者のためのコールドスタート手順です。

## 最初の原則

何も覚えていないものとして開始してください。

会話履歴、以前のモデルの記憶、Issueタイトル、断片的なコードより、現在のリポジトリ状態を優先します。重要な判断をチャットの中だけに残してはいけません。

## 読む順番

作業前に次の順で読みます。

1. `README.md`
2. `docs/PROJECT_PLAN.md`
3. `docs/ROADMAP.md`
4. `docs/HANDOFF.md`
5. 現在のIssue、Pull Request、CI状態

## プロジェクトの中心

FunaYomiは、芦屋ボートレースの過去データとオッズを用い、透明で再現可能な統計アルゴリズムによって各買い目の的中確率を推定し、期待値順に並べる研究用ウェブアプリを目指します。

中心式は次の通りです。

```text
期待回収率 = 推定的中確率 × オッズ
期待利益率 = 期待回収率 - 1
```

難所は期待値の掛け算ではなく、未来情報を混ぜずに的中確率を推定することです。

## 現在の固定方針

- 初期対象は芦屋、場コード `21`
- 初期賭式は3連単
- 初期データ源はTurnmark API
- 将来の当日情報候補はBoatrace Open API
- 有料AI API・LLM APIを予測に使わない
- 予測は説明可能な統計・数理アルゴリズムで行う
- 初期段階では自動投票を実装しない
- 利益保証や「必勝」の表現をしない
- 公式サイトの無許可高頻度スクレイピングを始めない
- 時系列分割を使い、未来情報の漏洩を防ぐ
- 条件を満たさないレースは「見送り」とする

## 現在の実装状態と権限

Issue #1 の明示的な許可により、M1〜M3と最小時系列バックテストの非UIコアを
2026-07-24に実装しました。山田さんの2026-07-27のGate X承認により、
Turnmark限定・retrospective・non-actionableな2連単schema、
program Plackett–Luce、市場blend、固定予算portfolio backtestも完了
しています。実際の状態、悪い結果を含む正式ledger、次の再開地点は
`docs/HANDOFF.md`、データ境界は `docs/DATA_CONTRACT.md` を正本とします。

新しいマイルストーン、UI、当日データ源、自動投票へは、現在のIssueや
山田さんの明示的な指示なしに拡張しないでください。既存コアのバグ修正、
テストは、その指示範囲内で連続して進められます。Gate U、結果を見た追加の
閾値・特徴・賭式探索、prospective収集は未承認です。

公式翌日番組LZHは廃案ではなく、将来prospective program snapshotが必要に
なった場合の候補としてHoldしています。利用条件とfield契約は未確認で、
収集は開始していません。現在の次工程として扱わないでください。

M1監査をやり直す必要はありません。ただし Turnmark の原本は後日変更される
ため、別SHAのデータを採用する場合は差分監査とデータ期間の記録が必要です。
オッズ時点は未確認のままであり、「最終」「締切」「前夜」と断定しては
いけません。

## 開発時の運用

山田さんへの認知コストを抑えるため、細かい実装単位ごとの承認は求めません。合意済みマイルストーンの境界内では連続して作業し、次の場合だけ確認を求めます。

- マイルストーンの開始または完了
- 初期スコープや中心仮説を変更するとき
- 新しい外部サービス、有料サービス、秘密情報を使うとき
- データ利用条件や法的・倫理的な懸念があるとき
- 自動投票、資金管理、アカウント操作へ近づくとき
- 複数の方向から技術では決められない価値判断が必要なとき

途中報告は、次の4点を日本語で簡潔にまとめます。

1. 何ができるようになったか
2. なぜ必要だったか
3. 現在の不確実性または危険箇所
4. 次の判断ゲート

## 検証規律

- テスト期間より未来の結果を学習に使わない
- 同一レースの結果・払戻・確定後情報を予測特徴へ入れない
- オッズ由来の市場確率を「独立した予測確率」と偽らない
- 高期待値表示だけでなく、確率較正とサンプル数を確認する
- 回収率だけでなく、購入数、最大連敗、最大ドローダウンを報告する
- 閾値を結果に合わせて何度も調整した場合、その試行を隠さない
- データ欠損、取消、不成立、同着を明示的に処理する

## 作業終了時

実質的な作業を終える前に、必ず次を行います。

- `docs/HANDOFF.md` を実際の状態に更新
- 完了したマイルストーンと未完了項目を更新
- 使用データ期間、テスト、失敗、未確認事項を正直に記録
- 次の再開地点を1つに絞って記録
- 重要な判断をチャットだけに残さない

リポジトリの文書言語は日本語を基本とします。コード識別子は一般的な英語で構いません。
